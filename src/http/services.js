import { services as auth } from '@/modules/auth'
import { services as home } from '@/views/home'
import { services as employee } from '@/views/employee'

export default {
  auth,
  home,
  employee
}
